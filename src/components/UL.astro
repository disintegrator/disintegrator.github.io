---
import type { HTMLAttributes } from "astro/types";

type Props = Omit<HTMLAttributes<"blockquote">, "class">;

const props = Astro.props;
---

<ul {...props} class="mb-4 last:mb-0 ml-4 space-y-2 list-disc list-outside">
	<slot />
</ul>
