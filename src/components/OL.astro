---
import type { HTMLAttributes } from "astro/types";

type Props = Omit<HTMLAttributes<"ol">, "class">;

const props = Astro.props;
---

<ol {...props} class="mb-4 last:mb-0 ml-4 space-y-2 list-decimal list-outside">
	<slot />
</ol>
